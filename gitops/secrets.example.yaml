# Template only — do not apply. Copy to secrets.yaml (gitignored) or create via kubectl:
#
#   influxdb3 create token --admin --offline --output-file admin-token.json
#   kubectl -n influxdb create secret generic influxdb-admin-token \
#     --from-file=admin-token.json=admin-token.json
#
apiVersion: v1
kind: Secret
metadata:
  name: influxdb-admin-token
  namespace: influxdb
type: Opaque
stringData:
  admin-token.json: |
    {"token":"<GENERATE_OFFLINE>","name":"admin","description":"GitOps admin token"}
